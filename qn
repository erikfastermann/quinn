package main imports [strconv] with builtin_check

a : [int int] string
a = {|x y|
	tmp = strconv_atoi y
	n = if (a < b) { 1 } else { 2 }
	[(x + tmp) (n + tmp)]
	for i in [a b c] {



	}
	b = {
		x
		y
	}
	a b | c () | d e
	a (b c | d | e f) g

	a
	(()-b)
	y = match x [
		[a 1] {}
		[b 2] {}
	]
}

a = b
(a b) = c
